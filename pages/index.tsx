import type { NextPage } from 'next'
import Head from 'next/head'
import Hero from './components/Hero'
import { Livvic } from 'next/font/google'
import Header from './components/Header'
import About from './components/About'

const livvic = Livvic({
  subsets: ['latin'],
  weight: ['500'],
  style: 'normal',
})

const Home: NextPage = () => {
  return (
    <div className="no-scrollbar  overflow-x-hidden">
      <Head>
        <title>Gonzalo Vidal - Portfolio</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Header />
      <section
        id="hero"
        className={`${livvic.className} font-sans flex items-center bg-[#f6f3ee]`}
      >
        <Hero />
      </section>

      <section
        id="about"
        className={`${livvic.className} z-50 font-sans flex items-center bg-[#e08e0a]`}
      >
        <About />
      </section>
    </div>
  )
}

export default Home
